%h1
	Editing Professor

- form_for(@professor) do |f|
	= f.error_messages
		
	%p
		= f.label :name
		%br= f.text_field :name
		
	%p= submit_tag('Update')
		
= link_to('Show', @professor)
= link_to('Back', professors_path)